- 自定义组件采用 `{ComponentName}_{DesignSystem}{Variant}{ID}` 格式命名（如 `Steps_DA01`），并在 `engine/editor-components/index.ts` 中统一注册。
- 渲染引擎中的自定义标签解析支持单行自闭合与多行块级两种语法，并通过 `extractBlock` 工具函数统一处理属性抽取与内容提取。
- 所有模式的工作台组件均接收 `colors` (ThemeColors) 作为 props，确保渲染输出的 HTML 能够动态响应用户的主题色切换。
- 状态管理中引入 `demoVersion` 与 `dirty` 标记机制，仅在示例版本变更且用户未手动编辑时自动同步最新示例内容，防止误覆盖用户数据。