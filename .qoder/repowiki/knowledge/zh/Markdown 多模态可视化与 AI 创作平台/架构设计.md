- 采用多应用并行架构：根目录 `src` (React/Vite) 与 `r-markdown` (Vue/Vite) 作为独立的前端渲染引擎，`html-anything` (Next.js) 承载 AI Agent 交互与云端部署能力。
- 逻辑复用通过跨模块共享核心工具实现：`markdownParser.ts`、`inlineFormat.ts` 及 `editor-components` 等渲染原语在 React 与 Vue 实现间保持语义对齐。
- 知识驱动开发：根目录 `.agent/skills` 集中管理设计规范与前端最佳实践，为各子应用的 UI 构建与代码质量提供统一的约束标准。