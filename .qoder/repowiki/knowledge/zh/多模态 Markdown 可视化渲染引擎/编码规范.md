- 所有模式视图均采用左右分栏布局，左侧为 `CodeEditor` 编辑区，右侧为实时渲染预览区。
- 自定义 Markdown 扩展统一使用 HTML 风格标签（如 `<p-title>`、`<cta>`）或 `:::` 语法块进行声明。
- 复杂排版场景（文档/卡片）均引入“隐藏测量容器”配合 `ResizeObserver` 动态获取元素真实高度以辅助分页。
- 主题色管理通过 `useTheme` 组合式函数生成 CSS 变量，并在根节点动态注入以实现全局样式切换。