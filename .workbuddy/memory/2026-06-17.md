# 2026-06-17 工作日志

## Word 模板填充功能评估报告

- 基于 `docs/A4文档Word导出调研与规划.md` 已有调研内容，完成了「指定 Word 模板 → 根据模板填充内容」功能链路的完整评估分析
- 评估涵盖：产品交互设计、AI 指令设计、模板解析与渲染、结合项目实际的可行性分析
- **核心结论**：推荐自研模板体系（JSON Schema + Markdown 骨架），而非解析真实 .docx 文件
- **优先级**：低-中，依赖 Word 基础导出（Task 1-8）先完成
- 报告产出：`docs/Word模板填充功能评估报告.md`

## 决策修正：复用现有标签体系 + 指令库模式

用户确认了最终落地方向，对原评估报告追加了「附录：决策修正与最终落地方向」章节：

1. **不另起模板引擎**——复用项目已有的「自定义标识/标签 → 解析/渲染」体系（如 `<page-break/>`、`<p-title>` 的同一套机制）
2. **AI 指令采用指令库形式**——对标自由画布 `PromptLibrary`（`src/modes/html/PromptLibrary.tsx`），不同指令对应不同文档风格，带缩略图预览
3. **封面页作为独立指令变体**——「含封面页」与「不含封面页」是两条独立指令；含封面页版本需 AI 额外生成：文档版本、编写者、编写时间、审核者、审核时间、文档状态、机密等级
4. **暂列为待办**——待 Word 基础导出（Task 1-8）完成后落实

关键代码位置参考：
- 自定义标签解析：`src/modes/document/documentModel.ts` 的 `classifyBlock()`
- 封面页识别：`src/modes/document/documentModel.ts` 的 `isCoverPageBlocks()`
- 指令库 UI 范本：`src/modes/html/PromptLibrary.tsx`
- A4 文档当前单条指令：`src/lib/aiGuide.ts` 的 `buildDocumentAiGuide()`

## 自定义标签解析模块抽象评估

- 审计了 A4 文档、卡片、长图文三个模式的标签解析逻辑
- **发现**：`splitMarkdownBlocks()` 和 `classifyBlock()` 在 `documentModel.ts` 和 `cardModel.ts` 中几乎逐行重复
- **结论**：可以且应该抽象——提取为 `src/engine/blockParser/` 独立模块，两个模式改为 import 调用
- **暂不实施**：`markdownParser.ts` 的 20+ 标签 if-else 渲染链，等模板填充功能有实际需求（需注册新标签）时再重构为注册表
- 文档产出：`docs/自定义标签解析模块抽象评估.md`
- 工作量：0.5 天，无前置依赖，有测试覆盖可保障无回归
